{% extends 'base.html.twig' %}

{% block title %}GroupMonitoringSettings{% endblock %}

{% block body %}
    <h1>GroupMonitoringSettings</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ group_monitoring_setting.id }}</td>
            </tr>
            <tr>
                <th>DaysCount</th>
                <td>{{ group_monitoring_setting.DaysCount }}</td>
            </tr>
            <tr>
                <th>PostsCount</th>
                <td>{{ group_monitoring_setting.PostsCount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('group_monitoring_settings_index') }}">back to list</a>

    <a href="{{ path('group_monitoring_settings_edit', {'id': group_monitoring_setting.id}) }}">edit</a>

    {{ include('group_monitoring_settings/_delete_form.html.twig') }}
{% endblock %}
