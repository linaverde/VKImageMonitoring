<form method="post" action="{{ path('group_monitoring_settings_delete', {'id': group_monitoring_setting.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ group_monitoring_setting.id) }}">
    <button class="btn">Delete</button>
</form>
