{% extends 'base.html.twig' %}

{% block title %}UserMonitoringSettings index{% endblock %}

{% block body %}
    <h1>UserMonitoringSettings index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>DaysCount</th>
                <th>PostsCount</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user_monitoring_setting in user_monitoring_settings %}
            <tr>
                <td>{{ user_monitoring_setting.id }}</td>
                <td>{{ user_monitoring_setting.DaysCount }}</td>
                <td>{{ user_monitoring_setting.PostsCount }}</td>
                <td>
                    <a href="{{ path('user_monitoring_settings_show', {'id': user_monitoring_setting.id}) }}">show</a>
                    <a href="{{ path('user_monitoring_settings_edit', {'id': user_monitoring_setting.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('user_monitoring_settings_new') }}">Create new</a>
{% endblock %}
