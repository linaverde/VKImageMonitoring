{% extends 'base.html.twig' %}

{% block title %}UserMonitoringSettings{% endblock %}

{% block body %}
    <h1>UserMonitoringSettings</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_monitoring_setting.id }}</td>
            </tr>
            <tr>
                <th>DaysCount</th>
                <td>{{ user_monitoring_setting.DaysCount }}</td>
            </tr>
            <tr>
                <th>PostsCount</th>
                <td>{{ user_monitoring_setting.PostsCount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_monitoring_settings_index') }}">back to list</a>

    <a href="{{ path('user_monitoring_settings_edit', {'id': user_monitoring_setting.id}) }}">edit</a>

    {{ include('user_monitoring_settings/_delete_form.html.twig') }}
{% endblock %}
