{% extends 'base.html.twig' %}

{% block title %}MonitoringRecord{% endblock %}

{% block body %}
    <h1>MonitoringRecord</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ monitoring_record.id }}</td>
            </tr>
            <tr>
                <th>Time</th>
                <td>{{ monitoring_record.Time ? monitoring_record.Time|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ monitoring_record.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('monitoring_record_index') }}">back to list</a>

    <a href="{{ path('monitoring_record_edit', {'id': monitoring_record.id}) }}">edit</a>

    {{ include('monitoring_record/_delete_form.html.twig') }}
{% endblock %}
